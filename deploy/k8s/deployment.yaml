# Generated by GitHub Copilot
apiVersion: apps/v1
kind: Deployment
metadata:
  name: credit-card-usage-api
  labels:
    app: credit-card-usage-api
spec:
  replicas: 2
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: credit-card-usage-api
  template:
    metadata:
      labels:
        app: credit-card-usage-api
    spec:
      containers:
        - name: credit-card-usage-api
          image: credit-card-usage-analysis-prediction:hardening-check
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8010
              name: http
          env:
            - name: ENVIRONMENT
              value: "production"
            - name: HOST
              value: "0.0.0.0"
            - name: PORT
              value: "8010"
            - name: UVICORN_WORKERS
              value: "1"
            - name: LOG_LEVEL
              value: "warning"
            - name: RATE_LIMIT_MAX
              value: "120"
            - name: RATE_LIMIT_WINDOW_SECONDS
              value: "60"
            - name: MAX_REQUEST_BYTES
              value: "1000000"
            - name: ENABLE_API_DOCS
              value: "false"
            - name: TRUSTED_HOSTS
              value: "credit-card-api.prod.internal,credit-card-api.prod.company.com"
            - name: CORS_ALLOW_ORIGINS
              value: "https://credit-card-portal.company.com"
            - name: REQUIRE_MODEL_FOR_READINESS
              value: "true"
            - name: AUTO_TRAIN_ON_STARTUP
              value: "true"
            - name: STARTUP_TRAIN_SAMPLES
              value: "5000"
            - name: STARTUP_TRAIN_SEGMENT_COUNT
              value: "6"
            - name: API_KEY
              valueFrom:
                secretKeyRef:
                  name: credit-card-usage-secrets
                  key: API_KEY
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health/liveness
              port: http
            failureThreshold: 30
            periodSeconds: 5
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: credit-card-usage-api
  labels:
    app: credit-card-usage-api
spec:
  selector:
    app: credit-card-usage-api
  ports:
    - name: http
      port: 80
      targetPort: 8010
  type: ClusterIP
